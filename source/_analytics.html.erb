<script>
  mojforms.analytics.setGoogleMeasurementId('<%= config.analytics %>');

  if(mojforms.cookiepolicy.analytics()) {
    console.log("TEMP DEV ONLY: google loading...");
    mojforms.analytics.add();
  }
  else {
    console.log("TEMP DEV ONLY: google denied!!");
    mojforms.analytics.remove();
  }
</script>
